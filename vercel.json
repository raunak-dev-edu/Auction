{
    "builds": [{
        "src": "auction/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "auction/wsgi.py"
        }
    ]
}